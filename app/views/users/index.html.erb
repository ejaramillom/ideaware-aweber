<h1>Usuarios</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Nombre</th>
    <th>Telefono</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.name %></td>
      <td><%= user.phone %></td>
    </tr>
  <% end %>
</table>
